@page
@model Permission.Pages.RolePermission.IndexModel

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PixiuRolePermission[0].RoleCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PixiuRolePermission[0].PermissionCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PixiuRolePermission[0].GroupCode)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.PixiuRolePermission) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RoleCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PermissionCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GroupCode)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
